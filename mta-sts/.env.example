# Cloudflare API token — Workers Scripts:Edit + Zone DNS:Edit + Zone Workers Routes:Edit
# Scoped to specific zones in the Cloudflare token settings
CF_API_TOKEN=your_cloudflare_api_token_here

# Optional overrides (per-run, not per-domain)
# MTA_STS_MODE=enforce
# MTA_STS_MAX_AGE=86400
